# 開發收尾檢查表

每一個功能、除錯階段或長對話準備結束時，都必須完成以下項目。這是固定動作，不因程式是否成功而省略。

## 1. 確認目前狀態

- V10 的 Daily／H4／H1 截圖與 Replay 是否全部使用 ETH；右上 SESSION 是否沒有 `USE ETH (...)` 警告。
- 記錄目前 V1／V4 的明確版號。
- 區分「程式已修改」、「靜態檢查通過」、「TradingView compile 通過」與「圖表視覺驗證通過」。
- 尚未實際驗證的結果不得寫成已完成。
- 記錄目前穩定版本、診斷模式、已知限制與下一個安全測試點。

## 2. 回顧整段對話

- 記錄本段工作原本要解決的問題。
- 記錄做過的重要修改及修改理由。
- 記錄錯誤假設、失敗修改、造成的現象與回復方式。
- 記錄哪些推測有證據支持，哪些仍未確認。
- 整理可重用的技術教訓與工作流程教訓。

## 3. 更新 Repository MD

- `TEST_RESULT.md`：實際測試條件、版號、symbol、timeframe、mode、結果。
- `KNOWN_BUGS.md`：尚未解決的問題、已排除項目、不可重複採用的失敗方案。
- `PROJECT_HISTORY.md`：重要決策、錯誤修改、rollback 與學習。
- `CHANGELOG.md`：真正保留下來的程式變更；已撤回的修改不可描述成現行功能。
- `TODO.md`：下一步必須是可單獨驗證的小步驟。
- 若規格或架構已改變，同步更新 `SMC_SPEC.md` 與 `DESIGN.md`。

## 4. 驗證修改範圍

- 執行 `git diff --check`。
- 檢查是否誤改無關檔案或覆蓋使用者既有修改。
- 確認程式內顯示版號與 MD 記錄一致。
- V10 及後續新架構版本必須確認最右上永久版本識別表可見，且截圖中的 build ID 與程式、MD 完全一致；沒有版號的截圖不可作為驗收證據。
- 確認 Weekly Bias 已整合在右上表的固定頂部區塊，左側沒有重複方向表；未來統計列不得覆蓋或取代 BUILD／Weekly Bias rows。
- V1 尚未單獨驗證成功前，不同步重寫 V4。
- 不把診斷用 bypass、`OFF` 或暫時顯示方案描述為功能完成。

## 5. 收尾回報

- 簡述完成內容。
- 列出實際驗證與尚未驗證項目。
- 列出更新過的程式與 MD。
- 說明目前已知問題及下一步。
- 只有使用者要求時才 commit／push；若已執行，附上 commit hash 與 push 狀態。

## 本規則的來源

2026-07-14 per-zone SETUP 除錯中，曾在根因未確認時同時修改 V1/V4，並把未驗證版本預設設為 `FULL`，導致 H1 完全不顯示。之後才 rollback 到 V1 `V1-PZ-01 / PZ OFF`、V4 `V4-PZ-02 / PZ OFF` 的穩定基準。

因此，往後收尾不只記錄成功結果，也必須記錄錯誤、失敗嘗試、rollback、證據與學習，避免下一段對話重複相同錯誤。
